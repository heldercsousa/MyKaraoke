<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:local="clr-namespace:MyVocaList.View"
			 xmlns:contracts="clr-namespace:MyVocaList.Contracts.DTOs.List;assembly=MyVocaList.Contracts"
			 xmlns:components="clr-namespace:MyVocaList.View.Components"
			 xmlns:domain="clr-namespace:MyVocaList.Domain;assembly=MyVocaList.Domain"
			 xmlns:behaviors="clr-namespace:MyVocaList.View.Behaviors"
			 x:Class="MyVocaList.View.SpotPage"
			 x:DataType="local:SpotPage"
			 Background="{StaticResource AppBackgroundGradient}"
			 NavigationPage.HasNavigationBar="False">

    <ContentPage.Behaviors>
        <behaviors:SmartPageLifecycleBehavior 
		    NavBar="{x:Reference CrudNavBar}"
		    MainContent="{x:Reference mainContentGrid}"
		    LoadDataCommand="{Binding LoadDataCommand}"
		    EnableAutoBypass="True"
		    MaxFailuresBeforeBypass="2" />

        <behaviors:SafeNavigationBehavior 
            x:Name="SpotFormNavigationBehavior"
            TargetPageType="{x:Type local:SpotFormPage}"
            EnableSmartStackNavigation="False"   
            DebounceMilliseconds="800" />

        <behaviors:SafeNavigationBehavior 
		    x:Name="BackNavigationBehavior"
		    EnableSmartStackNavigation="True"
		    DebounceMilliseconds="500" />
    </ContentPage.Behaviors>

    <Grid RowDefinitions="Auto,*,Auto">
        <components:HeaderComponent Grid.Row="0" Title="Locais" />
        <Grid Grid.Row="0" 
            InputTransparent="True"
            ZIndex="2000">  
            <!-- TESTE: Frame simples sem componente -->
            <Frame BackgroundColor="Red"
                 WidthRequest="80"
                 HeightRequest="80"
                 CornerRadius="40"
                 HorizontalOptions="End"
                 VerticalOptions="End"
                 Margin="16"
                 InputTransparent="False"
                 HasShadow="True">
                <Frame.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnTestFrameTapped" />
                </Frame.GestureRecognizers>
                <Label Text="TAP"
                     TextColor="White"
                     FontSize="24"
                     FontAttributes="Bold"
                     HorizontalOptions="Center"
                     VerticalOptions="Center" />
            </Frame>
        </Grid>
        <!-- CONTEÚDO (Grid 1) -->
        <Grid Grid.Row="1" x:Name="mainContentGrid">
            <components:CardWrapperComponent 
            TitleText="Cadastrados" 
            IconPath="spot_purple.png" 
            BadgeIsVisible="False"
            InputTransparent="False">
                <components:CardWrapperComponent.CardContent>
                    <Grid RowDefinitions="*">
                        <CollectionView x:Name="locaisCollectionView"
                          ItemsSource="{Binding Locais}"
                          SelectionMode="None"
                          BackgroundColor="Transparent">
                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="contracts:EstabelecimentoListItemDto">
                                    <Frame Style="{StaticResource SelectableCardListItemStyle}">
                                        <Frame.GestureRecognizers>
                                            <TapGestureRecognizer Tapped="OnItemTapped" />
                                        </Frame.GestureRecognizers>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <Label Grid.Column="0" 
                                    Text="{Binding Nome}" 
                                    Style="{StaticResource CardListItemMainTextStyle}"/>

                                            <Frame Grid.Column="1" 
                                    BackgroundColor="{StaticResource Error}" 
                                    CornerRadius="8" 
                                    Padding="6,3" 
                                    IsVisible="{Binding HasEvents}"
                                    Margin="8,0,0,0"
                                    HasShadow="False">
                                                <Label Text="COM EVENTOS" 
                                       FontSize="9" 
                                       TextColor="{StaticResource TextPrimary}" 
                                       FontAttributes="Bold"/>
                                            </Frame>
                                        </Grid>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>

                        <Frame x:Name="emptyStateFrame" 
                   IsVisible="False" 
                   Grid.Row="0" 
                   Style="{StaticResource EmptyQueueFrameStyle}">
                            <StackLayout Spacing="15" VerticalOptions="Start">
                                <Label Text="Nenhum local cadastrado" 
                           Style="{StaticResource EmptyQueueTitleStyle}" />
                                <Label Style="{StaticResource EmptyQueueTextStyle}">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="• Toque no botão " />
                                            <Span Text="+" TextColor="#FFD700" FontAttributes="Bold" FontSize="18" />
                                            <Span Text=" no canto inferior direito para cadastrar seu primeiro local" />
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                            </StackLayout>
                        </Frame>
                    </Grid>
                </components:CardWrapperComponent.CardContent>
            </components:CardWrapperComponent>
        </Grid>

        <!-- FAB (Grid 2 - IRMÃO do mainContentGrid) --><!--
        <Grid Grid.Row="1" 
          InputTransparent="True"
          ZIndex="1000">
            <components:FabButtonComponent 
            x:Name="addFab"
            IconText="+"
            IsVisible="True"
            Clicked="OnAddFabClicked"
            InputTransparent="False" />
        </Grid>-->

        <!-- FAB (Grid 2) -->
       

        <!-- NAVBAR -->
        <components:CrudNavBarComponent Grid.Row="2"
                                  x:Name="CrudNavBar"
                                  SelectionCount="{Binding SelectionCount}"
                                  ButtonClicked="OnCrudNavBarButtonClicked"/>
    </Grid>
</ContentPage>