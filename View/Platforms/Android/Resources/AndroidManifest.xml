<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <application 
        android:allowBackup="true"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config">
        
        <!-- Configuração específica para suporte a múltiplos idiomas -->
        <meta-data 
            android:name="android.app.default_locale" 
            android:value="pt-BR" />
            
        <!-- Força suporte a entrada multilíngue -->
        <meta-data 
            android:name="android.app.lib_name" 
            android:value="maui" />
    </application>
    
    <!-- Suporte explícito a múltiplos idiomas e entrada de texto -->
    <supports-screens 
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true" />
        
    <!-- Declaração de suporte a entrada multilíngue -->
    <uses-feature 
        android:name="android.software.input_methods" 
        android:required="false" />
</manifest>